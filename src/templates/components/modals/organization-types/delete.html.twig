<div class="modal fade" id="deleteModal_{{ organization_type.id }}" tabindex="-1" role="dialog" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteModalLabel">Are you sure?</h5>
                <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">×</span>
                </button>
            </div>
            <div class="modal-body">This operation will destroy all user data <br /><br />Select "Confirm" to proceed.</div>
            <div class="modal-footer">
                <form id="deleteForm" action="{{ path('organization_types_delete', {'id': organization_type.id} ) }}" method="post">
                    <button id="deleteSubmit" type="submit" class="btn btn-danger">Confirm</button>
                    <button class="btn btn-secondary" type="button" data-dismiss="modal">Cancel</button>
                </form>
            </div>
        </div>
    </div>
</div>

